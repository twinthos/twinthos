Site: /tmp/twinthos-site/

Files:
- assets/site.css — 1105 lines, dark theme (#000000 bg, #20F782 signal green, Inter font). ZERO scroll animations: no @keyframes, no scroll-triggered transforms, no fade/slide on scroll.
- assets/site.js — 166 lines. Nav burger toggle, scroll state (nav bg darkens on scroll), IntersectionObserver for .reveal class (but NO HTML elements use .reveal), signal green pulse on hero mark (scroll-based opacity), human-arch mark light (scroll-based opacity), conversion analytics.
- 7 HTML pages: index.html, about/index.html, book/index.html, security/index.html, operations-audit/index.html, privacy.html, 404.html — all use dark nav structure.

Current motion: only button hover/active scale (scale 0.97), nav-cta hover bg transition. That's it. The site is visually static.

Design direction: dark architectural, black/white/signal-green only, short copy, one idea at a time. Must NOT add bouncy/playful/spring animations — keep it restrained, industrial, confident. Animations should feel like slow-revealing, not dancing.

Recommended animation types: opacity fades, subtle translate-y (12-24px), parallax-lite on hero/human marks, smooth section transitions, staggered child reveals. Must respect prefers-reduced-motion. Must NOT slow down scroll (passive listeners, rAF throttling, IntersectionObserver where possible).

Task: Audit the current Twinthos site for animation and scroll-action opportunities. Return a numbered list of concrete, implementable animation additions with exact CSS/JS patches.

Focus areas:
1. Scroll-triggered reveal animations for sections/headlines/body text — currently only .reveal CSS exists but no HTML uses it. Which sections should get .reveal or .reveal-in? Should there be staggered delays on child elements?
2. Parallax-lite effects on hero mark and human-arch mark — the JS already has scroll-based opacity for these, but could add subtle translate-y parallax.
3. Smooth hover/active states on CTAs and nav — currently just scale 0.97 on active. Could add subtle translate-y on hover, glow effects on signal-green buttons.
4. Entrance animations on page load — hero content could fade/slide in on load.
5. Signal-green pulse refinements — the hero mark pulse is scroll-based opacity. Could be refined.

For each item give: title, description, priority (critical/high/medium/low), exact CSS patch (or "none"), exact JS patch (or "none"), HTML changes needed (or "none"), files affected.

Recommend 6-10 specific additions. Be critical — only recommend animations that improve the feel, not decoration. The site should feel modern and alive but not busy.
